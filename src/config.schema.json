{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RouteWatch Configuration",
  "description": "Configuration schema for routewatch CLI tool",
  "type": "object",
  "properties": {
    "appDir": {
      "type": "string",
      "description": "Root directory of the Next.js app router (relative to project root)",
      "default": "app"
    },
    "ignore": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of directory or file patterns to ignore during scanning",
      "default": ["node_modules", ".next", "dist"]
    },
    "output": {
      "type": "string",
      "enum": ["text", "json", "markdown"],
      "description": "Output format for the diff report",
      "default": "text"
    },
    "outputFile": {
      "type": "string",
      "description": "Optional file path to write the report output to"
    },
    "methods": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["GET", "POST", "PUT", "PATCH", "DELETE", "HEAD", "OPTIONS"]
      },
      "description": "HTTP methods to track in route files",
      "default": ["GET", "POST", "PUT", "PATCH", "DELETE", "HEAD", "OPTIONS"]
    }
  },
  "additionalProperties": false
}
